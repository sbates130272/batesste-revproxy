version: '3'
services:
  app:
    image: jc21/nginx-proxy-manager:2.9.19
    restart: unless-stopped
    ports:
      - '80:80'
      - '81:81'
      - '443:443'
    volumes:
      - ./data:/data
      - ./letsencrypt:/etc/letsencrypt

networks:
  ngnix-proxy-network:
    external: true
